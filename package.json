{
    "name": "secp256k1-zkp-js",
    "version": "1.0.0",
    "license": "MIT",
    "author": "",
    "scripts": {
        "configure": "npm run docker emconfigure ./configure src secp256k1-build -- --with-bignum=no",
        "make": "npm run docker emmake make",
        "test": "npm run lint -- --quiet && mocha -t 6000 test/**/*.js",
        "lint": "eslint src",
        "docker": "docker run -e DEBUG=1 -v $(pwd):/src --rm -t secp256k1-js"
    },
    "dependencies": {
        "create-hash": "^1.1.2",
        "long": "^3.2.0",
        "randombytes": "^2.0.3"
    },
    "devDependencies": {
        "eslint": "^6.7.2",
        "mocha": "^7.0.0"
    }
}
